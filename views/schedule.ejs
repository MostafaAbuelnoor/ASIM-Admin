<nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-primary">
  <div class="container">
    <a href="/dashboard" class="navbar-brand">ASIM</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive"
      aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav mr-auto">
        <div class="collapse navbar-collapse" id="navbarResponsive">
          <ul class="navbar-nav">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" id="themes">Actions<span
                  class="caret"></span></a>
              <div class="dropdown-menu" aria-labelledby="themes">
                <a class="dropdown-item" href="/docs">Doctors</a>
                <a class="dropdown-item" href="/appts">Appointments</a>
              </div>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/contact">Contact Us</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/about">About</a>
            </li>
          </ul>

          <a href="/users/logout" class="btn btn-secondary" style="margin-left: 600px;">Logout</a>
        </div>
</nav>

<div style="margin-top:10%; text-align: center;">
  <h1><%= doc[0]%> Schedule</h1>
  <hr class="my-4">
  <div class="row mt-5">

    <table class="table table-hover">
      <thead>
        <tr>
          <th scope="col">Doctor</th>
          <th scope="col">Specialization</th>
          <th scope="col">Date and Time</th>
          <th scope="col">Patient name</th>
          <th scope="col">Patient email</th>
          <th scope="col">Patient Number</th>
          <th scope="col">Patient No-show</th>

        </tr>
      </thead>

      <% for(var item in doc) {
          if(Object.values(pat[item]).length == 1 ) {
            var x 
            if(pat[item]['1'].noshow == '1') x = "Yes"
            else x ="No"
            %>

      <tbody>
        <tr class="table-primary">
          <td><%= doc[item] %></td>
          <td><%= spec[item] %></td>
          <td><%= dat[item] %></td>
          <td><%= pat[item]['1'].name %></td>
          <td><%= pat[item]['1'].email %></td>
          <td><%= pat[item]['1'].phone %></td>
          <td><%= x%></td>


          <% }
          else if(Object.values(pat[item]).length == 2){
            var x 
            if(pat[item]['2'].noshow == '1') x = "Yes"
            else x ="No"
            %>

      <tbody>
        <tr class="table-primary">
          <td><%= doc[item] %></td>
          <td><%= spec[item] %></td>
          <td><%= dat[item] %></td>
          <td><%= pat[item]['2'].name %></td>
          <td><%= pat[item]['2'].email %></td>
          <td><%= pat[item]['2'].phone %></td>
          <td><%= x%></td>

          <%}}%>
        </tr>
      </tbody>
    </table>
  </div>
</div>